// Hoisting

// Hoisting is the concept of using a variable/function before its declaration.

// For variables, the variables are set to undefined. Only variables with var can be
// accessed, not let & const.

// For functions, the whole functions are stored into memory.